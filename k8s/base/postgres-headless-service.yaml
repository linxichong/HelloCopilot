apiVersion: v1
kind: Service
metadata:
  name: hello-copilot-postgres-headless
spec:
  clusterIP: None
  selector:
    app: hello-copilot-postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
