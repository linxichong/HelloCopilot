apiVersion: v1
kind: Service
metadata:
  name: hello-copilot-postgres
spec:
  selector:
    app: hello-copilot-postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  type: ClusterIP
