apiVersion: v1
kind: Service
metadata:
  name: hello-copilot
spec:
  selector:
    app: hello-copilot-app
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  type: ClusterIP
