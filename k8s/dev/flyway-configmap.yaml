# Generated by scripts/generate-flyway-configmaps.sh. Do not edit by hand.
apiVersion: v1
kind: ConfigMap
metadata:
  name: flyway-sql-files
  namespace: study-dev
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: "-1"
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
data:
  V1__create_items.sql: |
    CREATE TABLE items (
        id BIGSERIAL PRIMARY KEY,
        name VARCHAR(120) NOT NULL,
        description TEXT,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );
    
    CREATE INDEX idx_items_created_at ON items (created_at DESC);

  V2__add_price_to_items.sql: |
    ALTER TABLE items
    ADD COLUMN price NUMERIC(10,2);
  V3__add_price2_to_items.sql: |
    ALTER TABLE items
    ADD COLUMN price2 NUMERIC(10,2);
  V4__drop_price2_from_items.sql: |
    ALTER TABLE items
    DROP COLUMN price2;

  V5__drop_price_from_items.sql: |
    ALTER TABLE items
    DROP COLUMN price;

  V6__add_age_to_items.sql: |
    ALTER TABLE items
    ADD COLUMN age INTEGER;

